query GetUserDetailsQuery {
    getUserDetails {
        id
        name
        lastName
        address
        zipcode
        city
        email
        phoneNumber
        user {
            id
            username
            email
        }
    }
}